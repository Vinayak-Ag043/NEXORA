function statusLabel(status) { return Number(status) === 0 ? 'Active' : Number(status) === 1 ? 'Passed' : 'Defeated'; }
export default function ProposalCard({ proposal, children }) { const total = proposal.yesCount + proposal.noCount; const yes = total ? Number((proposal.yesCount * 100n) / total) : 0; return <article className="proposal-card"><div className="proposal-card__top"><span className={`status status--${statusLabel(proposal.status).toLowerCase()}`}>{statusLabel(proposal.status)}</span><small>Ends {new Date(Number(proposal.deadline) * 1000).toLocaleString()}</small></div><h3>{proposal.title}</h3><p>{proposal.description || 'No description provided.'}</p><div className="vote-bar"><i style={{ width: `${yes}%` }} /></div><div className="vote-tally"><span>Yes <b>{proposal.yesCount.toString()}</b></span><span>No <b>{proposal.noCount.toString()}</b></span></div>{children}</article>; }
