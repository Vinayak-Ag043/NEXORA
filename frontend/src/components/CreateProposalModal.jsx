import { useState } from 'react';
export default function CreateProposalModal({ onClose, onCreate }) { const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [duration, setDuration] = useState('86400'); const [busy, setBusy] = useState(false); async function submit(event) { event.preventDefault(); setBusy(true); try { await onCreate(title.trim(), description.trim(), duration); onClose(); } finally { setBusy(false); } } return <div className="modal-backdrop" role="presentation"><form className="modal" onSubmit={submit}><button type="button" className="modal-close" onClick={onClose}>×</button><p className="eyebrow">New proposal</p><h2>Put it to a vote</h2><label>Title<input maxLength="128" value={title} onChange={(e) => setTitle(e.target.value)} required /></label><label>Description <span>(optional)</span><textarea maxLength="2048" value={description} onChange={(e) => setDescription(e.target.value)} /></label><label>Voting duration (seconds)<input type="number" min="300" max="2592000" value={duration} onChange={(e) => setDuration(e.target.value)} required /></label><button className="button button--primary" disabled={busy}>{busy ? 'Confirm in wallet…' : 'Create proposal'}</button></form></div>; }
