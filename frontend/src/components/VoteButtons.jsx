export default function VoteButtons({ proposal, onVote }) { const disabled = Number(proposal.status) !== 0 || proposal.hasVoted; return <div className="vote-buttons"><button className="button vote-yes" disabled={disabled} onClick={() => onVote(true)}>Yes</button><button className="button vote-no" disabled={disabled} onClick={() => onVote(false)}>No</button>{proposal.hasVoted && <small>You have voted</small>}</div>; }
