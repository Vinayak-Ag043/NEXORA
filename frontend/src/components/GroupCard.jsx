export default function GroupCard({ group, active, onSelect, onJoin }) { return <article className={`group-card ${active ? 'group-card--active' : ''}`}><button className="group-card__body" onClick={onSelect}><span>GROUP {group.id.toString().padStart(2, '0')}</span><h3>{group.name}</h3><p>{group.description || 'No description provided.'}</p><small>{group.memberCount.toString()} member{group.memberCount === 1n ? '' : 's'} · {group.creator.slice(0, 6)}…{group.creator.slice(-4)}</small></button>{group.isMember ? <button className="button button--quiet" onClick={onSelect}>View</button> : <button className="button button--primary" onClick={onJoin}>Join</button>}</article>; }
